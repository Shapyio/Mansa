{
  "name": "Mansa Dev",
  "dockerComposeFile": "../docker-compose.yml", // Use docker-compose
  "service": "app", // The name of your main container
  "workspaceFolder": "/app",
  "settings": {
    "python.defaultInterpreterPath": "/opt/venv/bin/python3", // Set python path
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": [
    "ms-vscode.cpptools",
    "ms-vscode.cmake-tools",
    "ms-python.python"
  ],
  "forwardPorts": [
    5432
  ],
  "runArgs": [ // Add env to container 
    "--env-file",
    "secrets.env"
  ],
  "postCreateCommand": "mkdir -p build && cmake -S . -B build",
  "shutdownAction": "stopCompose"
}